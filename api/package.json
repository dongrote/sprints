{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "db:migrate": "sequelize-cli db:migrate --config db/config/config.js --migrations-path db/migrations",
    "start": "node ./bin/www",
    "test": "npm run unit",
    "unit": "NODE_ENV=test mocha --recursive --reporter=min -b",
    "unit-verbose": "NODE_ENV=test mocha --recursive",
    "unit-watch": "NODE_ENV=test mocha --recursive --reporter=dot -b -w",
    "unit-watch-verbose": "NODE_ENV=test mocha --recursive -b -w"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug-logger": "^0.4.1",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "http-error-constructor": "^0.1.0",
    "lodash": "^4.17.20",
    "morgan": "~1.9.1",
    "sequelize": "^6.3.5",
    "sequelize-cli": "^6.2.0",
    "socket.io": "^3.0.4",
    "sqlite3": "^5.0.0"
  },
  "devDependencies": {
    "expect": "^26.6.2",
    "mocha": "^8.2.1",
    "simple-mock": "^0.8.0"
  }
}
